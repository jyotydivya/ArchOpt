"""
ml.training
===========
Person 2 training utilities.

  mock_graph.py  mock / synthetic CampusGraphs until Person 1's build_graph() lands
  loss.py        self-supervised layout losses (PyTorch)
  train.py       training loop → ml/models/weights/campus_model.npz
  evaluate.py    population diagnostics (overlap, bounds, diversity)
"""
