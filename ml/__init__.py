"""
ml
==
Machine-learning side of ArchOpt.

  ml.dataset    — Person 1: campus graph builder (build_graph)
  ml.models     — Person 2: GNN encoder, layout decoder, campus model
  ml.training   — Person 2: training loop, losses, evaluation, mock graphs
  ml.inference  — Person 2: generate_candidates() entry point used by the backend
"""
